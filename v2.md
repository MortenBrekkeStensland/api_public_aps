## APS API v2

API v2 is supported by APS 4.0 and later. Fields and commands added after APS 4.0 are marked with their minimum version where relevant. Clients should tolerate unknown fields and missing optional fields so they remain compatible across APS releases and platforms.


### Protocol:
APS has a TCP server that waits for a client connection on a default port (31600) and can be configured from the app settings. Once a connection is instantiated between the client and APS it will continuously listen to the incoming messages from that client.
APS uses length-prefixed JSON messages for all sent/received API traffic. API v1 is no longer supported in current APS versions.

For sending commands
1. Prepare the command as JSON string.
2. Calculate the length of the entire message.
3. Send the length of the entire message in 4 bytes big-endian.
4. Send the JSON string message.

Clients can optionally send an API version message to indicate their supported version:
```JSON
{
   "command": "api_version", 
   "api_version": 2
}
```
APS also will send the API version once the connection instantiated
```JSON
{
   "action": "api_version", 
   "api_version": 2
}
```
APS also sends [`aps_info`](#aps-info) app metadata once the connection is established.
APS 4.3 and later on macOS and Windows also include the current app settings in the full state snapshot sent after connection. See [Settings](#settings) for the payload and the writable subset.

**Notes**

1. All the above messages are length-prefixed.
2. APS expects the API v2 protocol for client communication. If the `api_version` command is omitted, APS still uses the v2 protocol.
3. The `api_version` message is used to report version compatibility between APS and the connected client.



### Commands:

#### Commands structure
```JSON
{
   "command": "...", 
   "parameters": {
      "param1": "...",
      "param2": "...",
   }
}
```
Example:
```JSON
{
   "command": "OpenStart_Presentation", 
   "parameters": {
      "file_path": "C:\\1.pptx",
      "slideNr": 1,
      "isFullscreen": true
   }
}
```

---

#### Locality proof

APS 4.5 and later can prove whether a connected client runs on the same operating-system
machine without transmitting a stable hardware identifier. Support is advertised by the
`locality_proof` entry in `aps_info.data.capabilities`.

After receiving that capability, a client may send:

```json
{
   "command": "GetLocalityProof",
   "parameters": {
      "challenge": "000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f"
   }
}
```

`challenge` must be exactly 32 cryptographically random bytes encoded as 64 lowercase
hexadecimal characters. APS responds with:

```json
{
   "action": "locality_proof",
   "data": {
      "challenge": "000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f",
      "hmac": "5b0a4417bdfd828789a7c1f8187a18cbf12027af14636f43a9c243168d5db66c"
   }
}
```

The response echoes the exact challenge so a client can associate asynchronous feedback
with the correct connection attempt. `hmac` is 32 bytes encoded as 64 lowercase
hexadecimal characters and is calculated as:

```text
HMAC-SHA256(
  key = UTF8(normalizedMachineId),
  message = UTF8("aps-locality-hmac-v1") || hexDecode(challenge)
)
```

APS-Mac uses `IOPlatformUUID` from `IOPlatformExpertDevice`; APS-PC uses `MachineGuid`
from `HKLM\SOFTWARE\Microsoft\Cryptography`. The raw identifier is never sent over the
network. Normalize either source by trimming whitespace, removing one enclosing brace
pair, removing existing hyphens, requiring exactly 32 hexadecimal digits, lowercasing,
and reformatting it as a hyphenated `8-4-4-4-12` UUID. A missing or malformed machine
identifier, challenge, or parameter object produces no `locality_proof` feedback.

Cross-platform test vector:

```text
macOS raw:  A1B2C3D4-E5F6-47A8-90AB-1234567890CD
Windows raw: {a1b2c3d4-e5f6-47a8-90ab-1234567890cd}
normalized: a1b2c3d4-e5f6-47a8-90ab-1234567890cd
challenge:  000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f
HMAC:       5b0a4417bdfd828789a7c1f8187a18cbf12027af14636f43a9c243168d5db66c
```

Clients must generate a new challenge for every connection, accept only the exact
outstanding echoed challenge, and treat an absent or timed-out response as unsupported.
Unknown commands remain ignored by older APS versions.

---

#### Presentations file commands
1. `Navigation_NextFS`: Open the next presentation using the focused app in fullscreen
   - Parameters:
      - slideNr (int)
2. `Navigation_PrevFS`: Open prev presentation using the focused app in fullscreen
   - Parameters:
      - slideNr (int)
3. `Navigation_NextNoFS`: Open the next presentation using the focused app without fullscreen. With Speedf as the active PDF application, APS sends `open` with `isFullscreen: false`, leaving Audience output hidden.
4. `Navigation_CurrentFS`: Set current presentation in fullscreen. With Speedf as the active PDF application, this enables Audience Live for an already-prepared Presenter preview.
5. `OpenStart_Presentation`: open presentation by path
   - Parameters:
      - file_path (string)
      - slideNr (int)
      - isFullscreen (bool): When `false`, open without fullscreen. With Speedf selected, this loads the PDF without showing the Audience presentation.
6. `OpenStart_Presentation_Slot`: open presentation by slot number
   - Parameters:
      - slot (int)
      - slideNr (int)
      - isFullscreen (bool): When `false`, open without fullscreen. With Speedf selected, this loads the PDF without showing the Audience presentation.
7. `CapturePresentationSlot`: Capture current focused presentation and save its path to a slot
   - Parameters:
      - bank_number (int)
8. `SetPresentationSlotPath`: Update slot path by a given full path
   - Parameters:
      - slot (int)
      - file_path (string)

When `aps_info.data.capabilities` contains `presentation_preparation_feedback`, commands in this group that select a new presentation publish [`presentation_preparing`](#presentation-preparing-feedback). See that feedback contract for the exact included and excluded command types.

---

#### Presentation close commands
1. `Navigation_CloseOthers`: Close all other native presentation documents while retaining the current presentation.
   - The command has no parameters.
   - With Speedf selected on Windows, the existing platform-specific behavior closes the active PDF through Speedf's control API and keeps Speedf running; older Speedf builds fall back to application exit.
2. `CloseCurrentPresentation`: Close the native presentation document APS currently reports as displayed, without saving unsaved changes. This command is available when `aps_info.data.capabilities` includes `close_current_presentation`.
   - The target is the same supported native presentation APS uses for `any_presentation_displayed.data.is_any_presentation_displayed` at the time the command is handled.
   - APS closes only that presentation document and leaves its presentation application running.
   - If no supported native presentation matches the displayed state, APS does nothing. A browser or Google Slides presentation is not a native presentation target for this command.
   - The command has no parameters and sends no dedicated success or error response. Clients observe the resulting ordinary presentation-state feedback.

The command names above are established wire names. Clients must send them exactly as documented.

---

#### Presentations slide commands
1. `Key_Left`: Move one manual step backward in the active presentation application. Where supported, this rewinds one animation build before leaving the slide.
2. `Key_Right`: Move one manual step forward in the active presentation application. Where supported, this consumes one animation build before leaving the slide.
3. `Key_Esc`: Exit presentation/fullscreen mode in the active presentation application. With Speedf as the active PDF application, this disables Audience Live while keeping the PDF open and navigable in Presenter view.
4. `Key_B`: Toggle the presentation black screen where supported.
5. `Generic`: Go to a specified slide in the active presentation application.
   - Parameters:
      - slideNr (int)
6. `Powerpoint_Go`: Go to a specified slide (PowerPoint)
   - Parameters:
      - slideNr (int)
7. `Powerpoint_Section_Go`: Go to the first slide of a specified 1-based PowerPoint section. Available in APS 4.3 and later on macOS.
   - Parameters:
      - section (int): The 1-based section index. It must be greater than zero.
8. `Powerpoint_Section_Name_Go`: Go to the first slide of a PowerPoint section with a specified name. Available in APS 4.3 and later on macOS.
   - Parameters:
      - section_name (string): The section name. APS trims outer whitespace and matches names case-insensitively. If multiple sections have the same name, the first section in presentation order is used.
9. `Powerpoint_Section_Previous`: Go to the first slide of the section before the current PowerPoint section. Available in APS 4.3 and later on macOS. If the current slide is in the first section, no navigation occurs.
10. `Powerpoint_Section_Next`: Go to the first slide of the section after the current PowerPoint section. Available in APS 4.3 and later on macOS. If the current slide is in the last section, no navigation occurs.
11. `Powerpoint_Previous`
12. `Powerpoint_Next`
13. `Acrobat_Go`: Go to a specified slide (Adobe Acrobat DC Pro)
   - Parameters:
      - slideNr (int)
14. `Acrobat_Previous`
15. `Acrobat_Next`
16. `Keynote_GO` Go to a specified slide (Keynote)
   - Parameters:
      - slideNr (int)
17. `Keynote_Previous`
18. `Keynote_Next`

PowerPoint section commands do nothing when PowerPoint is not running, no presentation is open, section information is unavailable, or the requested target section does not exist or is empty. Previous/next section navigation never wraps beyond the first or last section.

The generic slide commands work with the presentation application APS is currently controlling, including PowerPoint, Keynote, supported PDF applications, and Google Slides. Google Slides uses the existing generic commands; it does not require separate Google Slides command names. Google Slides control and slide feedback require APS 4.3 or later.

---

#### PowerPoint media commands

> **Note:** These commands are only available when controlling PowerPoint on Windows.

1. `Presentation_Media_Control`: Control embedded media on the current PowerPoint slide.
   - Parameters:
     - `action` (string): `play`, `pause`, `stop`, or `toggle` (play/pause).
2. `Presentation_Media_Seek`: Seek within the embedded PowerPoint media.
   - Parameters:
     - `direction` (string): `forward` or `backward`.
     - `Milliseconds` (int): number of milliseconds to seek.

---

#### Presentation folder commands
1. `SetSelected_PresentationFolder`: Change wathed presentation folder
   - Parameters:
      - bank_number (string | int): it can be "Next", "Previous", or the bank number
2. `CaptureFolder`: Capture current focused presentation's folder and save its path to a bank
   - Parameters:
      - bank_number (int)
3. `SetPresentationFolderPath`: Update a presentation-folder bank by a given full path. Available when `aps_info.data.capabilities` includes `set_presentation_folder_path`.
   - Parameters:
      - bank_number (int)
      - folder_path (string)

#### Images commands
1. `Freeze`
2. `DisplayTest`
3. `Blackout`
4. `ExitImages` 
5. `Capture_Image`: Take a screenshot and save it to a bank
   - Parameters:
      - bank_number (int)
6. `Display_Image`: Display an image from a bank
   - Parameters:
      - bank_number (int)
7. `states`: Request still images states feedback, it will be sent by APS after this command
8. `SetImageSlotPath`: Update image-bank path by a given full path
   - Parameters:
      - bank_number (int)
      - file_path (string)
9. `Fade_StillImages`: Toggle the Still Images transition between Fade and Cut.
10. `FadeOn_StillImages`: Select Fade. This command is idempotent.
11. `FadeOff_StillImages`: Select Cut. This command is idempotent.
12. `SetFadeTime_StillImages`: Set the Still Images fade duration.
   - Parameters:
      - `fade_time` (number): Seconds from `0.10` through `5.00`, inclusive.

The Still Images transition commands are available when `aps_info.data.capabilities` includes `transition_control_feedback`. Every recognized transition command is followed by authoritative `imagesstates` feedback. A missing, Boolean, string, non-finite, or out-of-range `fade_time` leaves the saved value unchanged and resends the current state.

---

#### Media Player commands
1. `Play_MediaPlayer`
2. `Pause_MediaPlayer`
3. `Restart_MediaPlayer`
4. `Stop_MediaPlayer`
5. `Loop_MediaPlayer`
6. `Fade_MediaPlayer`: Toggle the Media Player transition between Fade and Cut.
7. `HoldAtEnd_MediaPlayer`: Toggle whether playback holds on the final video frame.
8. `Load_MediaPlayer`
   - Parameters:
      - bank_number (string | int): it can be "Next", "Previous", or the bank number
9. `MediaPlayer_Position`: in seconds
   - Parameters:
      - video_seconds (int)
10. `MediaPlayer_Forward`: in seconds
   - Parameters:
      - video_seconds (int)
11. `MediaPlayer_Rewind`: in seconds
   - Parameters:
      - video_seconds (int)
12. `SetMediaSlotPath`: Update media-bank path by a given full path
   - Parameters:
      - bank_number (int)
      - file_path (string)
13. `FadeOn_MediaPlayer`: Select Fade. This command is idempotent.
14. `FadeOff_MediaPlayer`: Select Cut. This command is idempotent.
15. `SetFadeTime_MediaPlayer`: Set the Media Player fade duration.
   - Parameters:
      - `fade_time` (number): Seconds from `0.10` through `5.00`, inclusive.

The Media Player transition commands are available when `aps_info.data.capabilities` includes `transition_control_feedback`. Every recognized transition command is followed by authoritative `MediaPlayer` feedback. A missing, Boolean, string, non-finite, or out-of-range `fade_time` leaves the saved value unchanged and resends the current state.

When `aps_info.data.capabilities` includes `media_player_operation_result`, APS also publishes [`media_player_operation_result`](#media-player-operation-result) after attempts to select media for a slot or load media. This feedback may follow a command from a network client or an operation performed in the local APS user interface. Clients must not wait for this message when the capability is absent.

Static images have no playback transport. While an image is displayed, `Pause_MediaPlayer`, `Restart_MediaPlayer`, `MediaPlayer_Position`, `MediaPlayer_Forward`, and `MediaPlayer_Rewind` do not pause, restart, or seek it. The image remains displayed and reports `playing` until it is stopped or replaced. `Loop_MediaPlayer` can still change the saved Media Player loop setting, but it does not create a timed playback loop for a displayed image.

---

#### Media folder commands
1. `SetSelected_MediaFolder`: Change wathed media folder
   - Parameters:
      - bank_number (string | int): it can be "Next", "Previous", or the bank number

---

#### Web Browser Commands (Chrome)

1. `OpenWebPage`: Launches the APS-controlled Google Chrome instance with remote debugging enabled and an isolated APS browser profile.
   - Parameters:
      - url (string): The URL to open.
      - fullscreen (bool): If `true`, the browser opens in fullscreen mode.
      - new_tab (bool): Retained for compatibility but currently ignored. APS searches its controlled tabs for the requested address first. If a matching tab exists, APS activates it without navigating or reloading it; otherwise, APS opens the address in a new tab. Google Slides tabs are matched by their stable presentation ID, so editor, presenter, account-prefixed, sharing, and slide-specific URL variants for the same presentation are reused.
2. `CloseWebBrowser`: Closes the Chrome instance started by `OpenWebPage`.
3. `SwitchTab`: Activates a specific browser tab using its remote debugging ID.
   - Parameters:
     - tabId (string): The debugging ID of the tab (e.g., `6689AD7376BBF72D319C4F5D4E16F10E`).
       You can obtain tab IDs from the [Webpage tabs feedback](#webpage-tabs).
4. `DeleteTab`: Closes a specific browser tab using its debugging ID.
   - Parameters:
     - tabId (string): The debugging ID of the tab (e.g., `6689AD7376BBF72D319C4F5D4E16F10E`).
       You can obtain tab IDs from the [Webpage tabs feedback](#webpage-tabs).
5. `RefreshTab`: Reloads a specific browser tab using its debugging ID.
   - Parameters:
     - tabId (string): The debugging ID of the tab (e.g., `6689AD7376BBF72D319C4F5D4E16F10E`).
       You can obtain tab IDs from the [Webpage tabs feedback](#webpage-tabs).
       Use `all` to reload all eligible browser tabs, equivalent to the app's `Ctrl+Shift+Alt+6` refresh-all shortcut.
6. `RefreshCurrentTab`: Reloads the currently active browser tab.
7. `CloseBrowserAndClearAllData`: Closes the browser and clears **all** browsing data, including sessions, cookies, history, and local storage.

When `aps_info.data.capabilities` contains `webpage_preparation_feedback`, `OpenWebPage`, `SwitchTab`, and browser fullscreen operations publish [webpage preparation feedback](#webpage-preparation-feedback).

---

#### General Commands
1. `Clear`: Clear slot or bank content
   - Parameters:
      - bank_number (string | int): can be "All" or integer
      - clear_type_key (string): can be one of these values [StillImages, Media, SlotPresentations, PresentationFolders, MediaFolders]
2. `ActivateApplication`: Brings the selected application to the foreground and activates its window.
   - Parameters:
      - application (string): it can be PowerPoint, PDF, or Webpage
3. `GetSettings`: Requests the current APS settings and installed presentation-app status. Available in APS 4.3 and later on macOS and Windows.
   - Parameters: none
   - Response: one [`settings`](#settings) feedback message
4. `SetSetting`: Sets one writable APS setting to an explicit value. Available in APS 4.3 and later on macOS and Windows.
   - Parameters:
      - `setting` (string): A writable setting name from the [Settings](#settings) table.
      - `value` (bool | string | object): The exact JSON type and allowed value documented for that setting.
   - Response: one [`settings_update_result`](#settings-update-result) message followed by one authoritative [`settings`](#settings) message.
5. `ToggleSetting`: Toggles one Boolean APS setting. Available in APS 4.3 and later on macOS and Windows.
   - Parameters:
      - `setting` (string): One of `seamless_switching`, `toggle_images_on_off_with_one_button`, `google_slides_use_presenter_view`, or `automatically_check_for_updates`. Windows also supports `hold_at_end_of_presentation` and `link_presentations`; macOS also supports `powerpoint_hide_presenter`.
   - Do not include `value`. Enum and display settings must use `SetSetting`.
   - Response: one [`settings_update_result`](#settings-update-result) message followed by one authoritative [`settings`](#settings) message.
6. `regenerateInstanceId`: Generates and persists a new random identifier for this APS installation. Available on macOS and Windows.
   - Parameters: none
   - Response: one `instanceIdRegenerated` message containing the new `instanceId`.

Request:

```json
{
  "command": "regenerateInstanceId"
}
```

Response:

```json
{
  "action": "instanceIdRegenerated",
  "data": {
    "instanceId": "7f27dc2e-e189-4ab8-9e52-642ea12b808c"
  }
}
```

`regenerateInstanceId` is intended only for APS Hub's explicit, user-confirmed clone-resolution flow. Like the rest of the existing LAN control API, this command is unauthenticated: any client with API access can reset the installation identity. APS logs every invocation with the old and new identifiers.

Example explicit write:

```json
{
  "command": "SetSetting",
  "parameters": {
    "setting": "seamless_switching",
    "value": true
  }
}
```

Example one-button toggle:

```json
{
  "command": "ToggleSetting",
  "parameters": {
    "setting": "seamless_switching"
  }
}
```

---

### Feedback:
Once the client gets connected, APS will continuously send info in length-prefixed JSON string.
> Some feedbacks will be sent only after getting triggered by commands like `capture`, `display`

#### Feedback structure
```JSON
{
   "action": "...",
   "data":{
      "data_key1": "...",
      "data_key2": [],
   }
}
```
- action (string): for specifying the type of this feedback
- data (object): it can containes any key-value pairs as required by the feedback, values can be any data type.

---

#### Generic notification feedback

The `notification` action is an additive, operator-facing notification channel. APS may send it when `aps_info.data.capabilities` contains `notification_feedback`. Clients must feature-detect that capability; a missing capability means this feedback is unsupported.

Notifications are informational presentation data. A client may display or dismiss them, but it **must not** use `area`, `level`, `message`, other additive fields, or the presence or absence of a notification to block controls, retry an operation, determine readiness, or make another business-critical decision. Any state or operation outcome that clients must interpret programmatically requires its own structured feedback contract.

Only `message` is required. APS can send a simple notification without first defining a code or a component-specific schema:

```json
{
  "action": "notification",
  "data": {
    "message": "The presentation application is responding again."
  }
}
```

A notification can optionally identify its program area and presentation importance:

```json
{
  "action": "notification",
  "data": {
    "area": "media_player",
    "level": "error",
    "message": "APS cannot access media folder 2."
  }
}
```

Fields:

| Field | Requirement | Contract |
|---|---|---|
| `message` | Required | Platform-neutral human-readable text of 1 to 2048 UTF-8 bytes. It contains enough information for a generic client to display the notice without recognizing any other field. |
| `area` | Optional | Open program-area identifier. Initial well-known values are `general`, `presentations`, `media_player`, `still_images`, and `webpages`. An absent or unknown value is presented as a general APS notification. New values do not require an API revision. |
| `level` | Optional | Presentation importance: `info`, `warning`, or `error`. When omitted or unknown, clients use `info`. It is never an instruction to apply control logic. |

Notifications are one-time events sent when the occurrence happens. They are not replayed in the connection snapshot and have no acknowledgement, dismissal, update, or resolution command. Dismissal is local client UI state and does not change APS state. APS should emit a later notification when a recovery is useful to the operator, for example that an unavailable folder is accessible again.

Clients identify the originating APS machine from the connection that delivered the frame. They should record their own receipt time when a display timestamp is useful. APS should avoid repeatedly sending the same unchanged notice in a tight loop; rate limiting and suppression remain implementation details rather than wire state that clients must manage.

Clients must tolerate unknown `area` values and additive fields in the feedback envelope or `data` object. APS-Mac and APS-PC can therefore send new messages from new program areas using this same contract without requiring a public API or client release. A malformed notification should be ignored without disconnecting an otherwise healthy APS connection.

Notification text and additive fields must be safe to expose to a remote operator. APS must not include credentials, licence keys, subscription identifiers, customer data, full filesystem paths, raw platform errors, or other secrets. Prefer a filename or a human-safe label when file context is useful.

Generic notifications do not replace authoritative state feedback or structured operation results. For example, [`media_player_operation_result`](#media-player-operation-result) remains the contract for Media Player selection and load outcomes, and ordinary [`MediaPlayer`](#media-player-feedback) remains the authoritative Media Player state. APS should not also publish a generic notification for the same occurrence. If a future use case requires both forms for compatibility, a separate additive contract must define a shared identifier and which feedback is authoritative.

---

#### Presentations Slots Feedback
```JSON
{
   "action": "slots",
   "data":{
      "filenames": [ "01.pptx", "02.pptx", "03.pptx", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-"],
      "exists": [true, true, true, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false],
      "opened": [true, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false]
   }
}
```

- `filenames`: An array of configured files in the `Presentations` tab
- `exists`:  An array to indicate if the file exists on the system or not
- `opened`:  An array to indicate if the file is currently opened or not

---

#### Files Feedback
```JSON
{
   "action": "files",
   "data":{
      "curr": "02.pptx",
      "prev": "01.pptx",
      "next":"-",
      "slide_number": "1",
      "slides_count": "10",
      "presenter_notes": "Welcome the audience and introduce the first topic.",
      "current_build": "0",
      "builds_count": "2",
      "powerpoint_slide_number": "1",
      "powerpoint_slides_count": "10",
      "powerpoint_current_build": "0",
      "powerpoint_builds_count": "2",
      "PowerPoint_media_duration": "00:19:50",
      "PowerPoint_media_current_position": "00:00:05",
      "PowerPoint_media_time_left": "00:19:45",
      "PowerPoint_media_state": "playing"
   }
}
```

- `curr`, `prev`, and `next`: Current, previous, and next presentation filenames. A value of `-` means no file is available.
- `slide_number` and `slides_count`: Current slide and total slide count for the presentation application APS is currently controlling.
- `presenter_notes`: Presenter notes for the current slide. Available in APS 4.3 and later on macOS when APS can read notes from PowerPoint, Keynote, or Google Slides. An empty string or `-` means no notes are available.
- `current_build` and `builds_count`: Current and total animation/build positions where available. Both values are strings. `builds_count` is the number of manual advances the current slide consumes before changing slides; `current_build` is the number already consumed. A slide with no builds reports `"0"` for both. A value of `"-"` means the information is unavailable.
- `powerpoint_slide_number`, `powerpoint_slides_count`, `powerpoint_current_build`, and `powerpoint_builds_count`: PowerPoint-specific state retained separately from the generic state above. These fields may be absent on older APS versions or unsupported platforms. On Windows they stay `"-"`, or retain the last PowerPoint state, while Google Slides is active. On macOS the two `powerpoint_` build fields currently mirror the generic build fields for PowerPoint, Keynote, and Google Slides. Clients should use the generic fields for the active presentation.

`build_feedback` is implemented on macOS and Windows with the shared contract above. APS-Mac 4.4 and later reports build fields for PowerPoint, Keynote, and Google Slides. Current APS-PC builds report them for PowerPoint and Google Slides. PDFs report `"-"` for both fields. Earlier APS-PC builds may provide numeric PowerPoint or Google Slides build fields without advertising the capability; clients retaining compatibility with those builds may feature-detect valid non-negative integer strings when the capability is absent.

For Google Slides, `builds_count` comes from the deck's server-side model and `current_build` is tracked in present mode. On macOS, `builds_count` is also available while the deck is active in the editor. On Windows, Google Slides build feedback is sent only while APS detects the presentation with its window in the foreground. The total may be `"-"` briefly when a deck is first seen and is refreshed when the slide count changes, when presentation mode starts, and within about a minute of an animation edit.

`current_build` remains `"-"` whenever APS cannot observe manual advances reliably; clients must then fall back to slide-only behavior. For Google Slides on both platforms this includes Presenter View, whose on-screen buttons are not fully observable; keyboard navigation in the Presenter View window remains observable, but APS still withdraws the cursor so clients cannot mistake partial observation for authoritative state. On macOS, missing Input Monitoring permission and a PowerPoint total based on an estimated live fallback also make the cursor unavailable. The build cursor resynchronizes at slide boundaries.

The `files` feedback is sent when presentation state changes, including every observed animation-build advance. In APS 4.3 and later, Google Slides uses the same `slide_number`, `slides_count`, and `presenter_notes` fields as the other presentation applications.

Fields specific to PowerPoint media:

> **Note:** These fields are only available when controlling PowerPoint on Windows.

- `PowerPoint_media_duration`: Total media duration (hh:mm:ss).
- `PowerPoint_media_current_position`: Current playback position (hh:mm:ss).
- `PowerPoint_media_time_left`: Remaining time (hh:mm:ss).
- `PowerPoint_media_state`: One of `playing`, `paused`, `stopped`, or `not_ready`.

---

#### Presentation Preparing Feedback

Available when `aps_info.data.capabilities` contains `presentation_preparation_feedback`. APS-Mac implements this feedback in APS 4.3 and later; APS-PC implements it in APS 4.4.0.1 and later.

Preparing a presentation:

```JSON
{
   "action": "presentation_preparing",
   "data": {
      "is_preparing": true,
      "command_type": "slot",
      "is_fullscreen": true,
      "slot": 1,
      "from": "/Presentations/Opening.pptx",
      "to": "/Presentations/Keynote.key",
      "watched_folder_number": 2,
      "from_watched_folder_index": -1,
      "to_watched_folder_index": 4
   }
}
```

Preparation finished:

```JSON
{
   "action": "presentation_preparing",
   "data": {
      "is_preparing": false,
      "command_type": "slot",
      "is_fullscreen": true,
      "slot": 1,
      "from": "/Presentations/Opening.pptx",
      "to": "/Presentations/Keynote.key",
      "watched_folder_number": 2,
      "from_watched_folder_index": -1,
      "to_watched_folder_index": 4
   }
}
```

- `is_preparing`: Required Boolean. `true` means APS has accepted a presentation start or switch request and the requested presentation is not visible on the presentation output yet. `false` means no preparation is currently in progress.
- `command_type`: Present on operation feedback. One of `direct_path`, `slot`, `next`, or `previous`. A direct path that also belongs to the watched folder remains `direct_path`; watched-folder membership does not change the command type.
- `is_fullscreen`: Present on operation feedback. The effective Boolean fullscreen choice for the operation.
- `slot`: Optional 1-based presentation slot number. It is present only when `command_type` is `slot`.
- `from`: Optional normalized absolute path of the presentation APS is switching away from. It is omitted when there is no previous presentation or APS cannot determine its path.
- `to`: Optional normalized absolute path of the presentation APS is preparing. For direct-path and slot commands it is normally present in the first preparing message. For next/previous navigation it may be omitted initially and added or replaced in a later `is_preparing: true` update after APS resolves the exact target.
- `watched_folder_number`: Optional 1-based selected presentation-folder bank captured when preparation starts.
- `from_watched_folder_index` and `to_watched_folder_index`: Zero-based positions matching the captured `watched_presentation_folder.files_list`. The corresponding field is present whenever its path is known and is `-1` when that path is not in the captured watched folder. Path identity, rather than filename alone, is used, so duplicate filenames in different directories remain distinct.

APS sends normalized absolute paths in `from` and `to`. Clients must replace their stored preparation context with every new `presentation_preparing` frame because an active frame can add or change the resolved target. Network commands and equivalent actions from the local APS user interface use the same feedback contract.

When Seamless Switching displays a screenshot cover, `is_preparing` remains `true` until both the presentation operation has reached its ready point and the cover has been removed. If Seamless Switching is disabled, unavailable, or its capture fails, the ready point alone ends the interval. APS sends the current state in the initial connection snapshot after advertising the capability; an idle snapshot contains only `is_preparing: false`.

Active updates and the final `is_preparing: false` edge retain all available operation context. The false edge reports that the preparation interval has ended; it is not a structured success result and must not be interpreted as proof that the requested presentation opened successfully. Commands that do not select a new presentation, such as `Navigation_CloseOthers`, `CloseCurrentPresentation`, escape, slide/build/section controls, and `Navigation_CurrentFS`, do not start this feedback. Google Slides uses [webpage preparation feedback](#webpage-preparation-feedback) instead.

---

#### PowerPoint Sections Feedback

Available in APS 4.3 and later on macOS. This feedback describes the sections in the PowerPoint presentation currently running as a slideshow.

```JSON
{
   "action": "powerpoint_sections",
   "data": {
      "available": true,
      "current_section_index": 2,
      "current_section_id": "id-2",
      "current_section_name": "Main section",
      "current_slide_in_section": 1,
      "sections_count": 2,
      "sections": [
         {
            "id": "id-1",
            "name": "Default Section",
            "first_slide_index": 1,
            "slides_count": 2
         },
         {
            "id": "id-2",
            "name": "Main section",
            "first_slide_index": 3,
            "slides_count": 2
         }
      ]
   }
}
```

- `available`: `true` when APS has section information for the active PowerPoint slideshow. An available presentation may legitimately contain zero sections.
- `current_section_index`: The current 1-based section index, or `-1` when the displayed slide cannot be mapped to a section.
- `current_section_id`: PowerPoint's opaque identifier for the current section, or `-` when unavailable.
- `current_section_name`: The current section name, or `-` when unavailable.
- `current_slide_in_section`: The displayed slide's 1-based position within its section, or `-1` when unavailable.
- `sections_count`: The number of section records in `sections`.
- `sections`: Presentation-ordered PowerPoint section records. A record's 1-based position in the array is its section index. Each record contains its opaque `id`, `name`, absolute 1-based `first_slide_index`, and `slides_count`.

When section information is unavailable, APS sends `available: false`, an empty `sections` array, `sections_count: 0`, `-1` for unavailable numeric current fields, and `-` for the current section ID and name. This clears stale client state when PowerPoint closes or leaves slideshow mode.

If APS cannot read any section record completely, it reports section information as unavailable instead of sending a partial section list.

The feedback is sent when section metadata, the current section position, or PowerPoint slideshow availability changes. It is also included in the full state snapshot sent after a client connects.

---

#### Images States Feedback
```JSON
{
   "action": "imagesstates",
   "data":{
      "displayTest": false,
      "displayBlack": false,
      "displayFreeze": false,
      "displayIndex": -1,
      "isLoaded": [ true, true, true, false, false, false, true, false, false, false],
      "filenames": ["logo.png", "Screenshot", "holding-slide.jpg", "-", "-", "-", "closing.png", "-", "-", "-"],
      "transition_type": "fade",
      "fade_time": 2.0,
      "transition_active": false
   }
}
```

- `displayTest`: Will be true if the color-bars screen is displayed
- `displayBlack`: Will be true if the black screen is displayed 
- `displayFreeze`: Will be true if the screen is freeze
- `displayIndex`: Zero indexed to show the index of the image from `Still Images` tab that is currently displayed, will be `-1` if none of them is displayed
- `isLoaded`: An array to indicate if an image is added to each slot or not
- `filenames`: A ten-entry array in bank order. A file-backed bank contains the image's filename, a captured-image bank contains `Screenshot`, and an empty bank contains `-`. This field contains filenames only, not full source paths.
- `transition_type`: `fade` or `cut`.
- `fade_time`: The configured fade duration in seconds as a JSON number.
- `transition_active`: `true` while a configured Still Images visual fade is actually running; otherwise `false`.

---

#### Media Player Feedback
```JSON
{
   "action":"MediaPlayer",
   "data":{
      "filenames":["video1.mp4","video2.mp4","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-","-"],
      "Media_playing":"1",
      "Media_loaded":"2",
      "Media_playing_filename":"video1.mp4",
      "Media_loaded_filename":"video2.mp4",
      "Media_playback_state":"playing",
      "Media_time_elapsed":"00:00:35",
      "Media_time_left":"00:19:15",
      "Media_duration":"00:19:50",
      "Media_player_loop_status":"off",
      "Media_player_fade_status":"on",
      "Media_player_hold_at_end_status":"off",
      "transition_type":"fade",
      "fade_time":2.0,
      "transition_active":true
      }
}
```
- `filenames`: An array of configured files in the `Media Player` tab
- `Media_playing`: The slot number of the currently playing media. When no media is active, macOS reports `-` and Windows reports the legacy value `None`.
- `Media_loaded`: The slot number of the currently loaded media. When no media is loaded, macOS reports `-` and Windows reports the legacy value `None`.
- `Media_playing_filename`: Filename of the currently playing media. When no media is active, macOS reports `-` and Windows reports the legacy value `None`.
- `Media_loaded_filename`: Filename of the currently loaded media. When no media is loaded, macOS reports `-` and Windows reports the legacy value `None`.
- `Media_playback_state`: One of `playing`, `paused`, or `None`.
- `Media_time_elapsed`, `Media_time_left`, and `Media_duration`: Playback times formatted as `hh:mm:ss`.
- `Media_player_loop_status`: `on` or `off`.
- `Media_player_fade_status`: `on` or `off`.
- `Media_player_hold_at_end_status`: `on` or `off`.
- `transition_type`: `fade` or `cut`. This is the typed equivalent of the retained `Media_player_fade_status` field.
- `fade_time`: The configured fade duration in seconds as a JSON number.
- `transition_active`: `true` while a configured Media Player visual fade is actually running; otherwise `false`. Cut operations, forced no-fade cleanup, and fixed audio-only stop fades do not activate it.

For a static image, `playing` means that the image is currently displayed. An image that is only loaded but not displayed does not become active media. While a slot-2 image named `image.png` is displayed, APS uses the existing fields without any image-specific API additions:

- `Media_playback_state`: `playing`
- `Media_playing`: `2`
- `Media_playing_filename`: `image.png`
- `Media_time_elapsed`, `Media_time_left`, and `Media_duration`: `00:00:00`

After the image is stopped, `Media_playback_state` is `None`. `Media_playing` and `Media_playing_filename` return `-` on macOS and the legacy value `None` on Windows.

The three typed transition fields are present when `aps_info.data.capabilities` includes `transition_control_feedback`. They are included in connection snapshots and ordinary scoped feedback. APS also sends scoped feedback immediately when transition settings change and on both edges of a visual fade. Overlapping fades keep `transition_active` true until the final fade completes. If the configured duration changes during a running fade, the current animation finishes with the duration captured when it started while feedback reports the newly saved `fade_time`.

---

#### Media Player operation result

When `aps_info.data.capabilities` contains `media_player_operation_result`, APS publishes the outcome of each Media Player selection or load attempt. The same platform-neutral error message is used by the local APS user interface and sent over the network.

Selection failure:

```json
{
  "action": "media_player_operation_result",
  "data": {
    "success": false,
    "operation": "select",
    "slot": 3,
    "filename": "clip.mxf",
    "error": {
      "code": "unsupported_file_type",
      "message": "This file type is not supported.",
      "details": {
        "file_extension": "mxf"
      }
    }
  }
}
```

Successful load:

```json
{
  "action": "media_player_operation_result",
  "data": {
    "success": true,
    "operation": "load",
    "slot": 3,
    "filename": "walk-in.mp4",
    "error": null
  }
}
```

- `success`: `true` when the selection or load succeeded; otherwise `false`.
- `operation`: `select` when assigning, replacing, or clearing a Media Player slot; `load` when preparing a slot for playback.
- `slot`: The 1-based Media Player slot number, or `null` when no valid slot can be identified.
- `filename`: The filename without its parent path, or `null` when no file can be identified. Clients must not treat this field as a usable filesystem path.
- `error`: `null` on success; otherwise an object containing a stable `code`, a human-readable `message`, and an optional `details` object. Clients must use `code`, not `message`, for program logic. Unknown additive fields and error codes must be tolerated.
- `error.details`: Optional platform-neutral string values that provide safe diagnostic context. Currently defined keys are `file_extension`, containing the lowercase extension without a leading dot, and `blocked_by`, containing `active_playback` or `automatic_loading`.

Error codes:

- `invalid_slot`: The requested slot is missing, malformed, or outside the supported range.
- `empty_slot`: The requested slot has no assigned media, or no assigned media is available for a Next/Previous load.
- `unsupported_file_type`: The selected file extension is not supported as video, audio, or image media.
- `file_not_found`: The assigned file no longer exists or cannot be found.
- `file_not_readable`: The file exists but APS cannot read it.
- `media_not_playable`: The file was found and read, but the media framework could not open it. The file may be damaged or use an unsupported codec.
- `operation_blocked`: APS rejected the operation because of current state, such as changing the active playing/paused slot or loading while an automatic load is pending.
- `internal_error`: APS could not complete the operation for another internal reason.

This is event feedback, not a request-correlated response: the payload has no command or request identifier, and local APS activity can also produce it. A client that initiated an operation may use `operation`, `slot`, and `filename` as context, but must continue to treat the ordinary [`MediaPlayer`](#media-player-feedback) message as the authoritative playback and slot state. Load results can arrive asynchronously after AV media validation. Older APS builds continue to work without this message; clients must feature-detect `media_player_operation_result` and retain their existing timeout or state-feedback behavior when it is unsupported.

This platform-neutral contract is implemented by APS-Mac and APS-PC.

---

#### Presentation Folders

```JSON
{
  "action": "presentation_folders",
  "data": {
    "names": ["Presentations", "PDF", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "PPT", "-"],
    "exists": [true, true, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, true, false]
  }
}
```

---

#### Watched Presentation Folder

```JSON
{
  "action": "watched_presentation_folder",
  "data": {
    "name": "Presentations",
    "number": 1,
    "files_list": [
      "C:\\Users\\userx\\OneDrive\\Presentations\\010.30 John Smith.pptx",
      "C:\\Users\\userx\\OneDrive\\Presentations\\11.00 Linda Jensen .pptx",
      "C:\\Users\\userx\\OneDrive\\Presentations\\11.30 Mike Johnsen.pptx"
    ]
  }
}
```
> **Note**: Paths are OS-dependent. It is the client’s responsibility to handle different path separators accordingly.

---

#### Any presentation displayed
This will be true if any presentation is displayed, regardless of whether it is in a slot, in the watched folder, or neither.
```JSON
{
  "action": "any_presentation_displayed",
  "data": {
    "is_any_presentation_displayed": true,
    "in_edit_mode": false
  }
}
```

- `is_any_presentation_displayed`: `true` when APS detects a supported presentation being displayed.
- `in_edit_mode`: `true` when the detected presentation is open but not in fullscreen presentation mode.

---

#### Opened folder presentation
If the currently displayed presentation is in the watched folder, its index will be returned; otherwise, the return value will be -1.
```JSON
{
  "action": "opened_folder_presentation",
  "data": {
    "current_opened_file_index": -1
  }
}
```

---

#### Media folders

```JSON
{
  "action": "media_folders",
  "data": {
    "names": ["Folder1", "-", "Folder3", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-", "-"],
    "exists": [true, false, true, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false, false]
  }
}
```

---

#### Watched media folder

```JSON
{
  "action": "watched_media_folder",
  "data": {
    "name": "Folder3",
    "number": 3,
    "files_list": [
      "C:\\Users\\userx\\Videos\\video1.mp4",
      "C:\\Users\\userx\\Videos\\video2.mp4"
    ]
  }
}
```
> **Note**: Paths are OS-dependent. It is the client’s responsibility to handle different path separators accordingly.

---

#### Image capture
Feedback to notify that a cupture (screenshot) is being taken right now, this is triggered after `Capture_Image` command
```JSON
{
   "action": "capture", 
   "index": 0
}
```

---

#### Slot capture
Feedback to notify that a slot path is being updated right now, this is triggered after `CapturePresentationSlot` command
```JSON
{
   "action": "slot_capture", 
   "index": 9
}
```

---

#### Folder capture
Feedback to notify that a presentation bank path is being updated right now, this is triggered after `CaptureFolder` command
```JSON
{
   "action": "folder_capture", 
   "index": 7
}
```

---

#### Webpage tabs
Provides information about all currently open tabs.
This feedback is triggered automatically whenever there is a change (e.g., tab opened, closed, or switched).

```json
{
   "action": "webpage_tabs",
   "data": {
      "active_tab_id": "782C53FB4C4FBC9CD58D7EC89B914F90",
      "is_foreground": true,
      "tabs": [
         {
            "id": "37CD32E964DEC7F5B3B8C16B75C86AC9",
            "url": "https://www.google.com/",
            "title": "Google"
         },
         {
            "id": "782C53FB4C4FBC9CD58D7EC89B914F90",
            "url": "https://www.presentationtools.com/",
            "title": "Home - PresentationTools"
         }
      ]
   }
}
```
- `active_tab_id`: The ID of the currently active tab.
- `is_foreground`: `true` if the Chrome window is currently in the foreground.
- `tabs`: A list of all open tabs (sorted alphabetically by title).
  - `id`: The unique identifier for the tab. This can be used with commands like `SwitchTab`, `DeleteTab`, and `RefreshTab`.

---

#### Webpage displayed
Indicates the display state of the browser window.
If the browser is focused, `is_any_presentation_displayed` will be `true`.
If it is in fullscreen mode, `in_edit_mode` will be `false`.
```json
{
   "action": "webpage_displayed", 
   "data": {
      "is_any_presentation_displayed": true,
      "in_edit_mode": false
   }
}
```

---

#### Webpage preparation feedback

Available when `aps_info.data.capabilities` contains `webpage_preparation_feedback`. APS-Mac implements this feedback in APS 4.3 and later; APS-PC implements it in APS 4.4.0.1 and later.

APS uses two existing actions for independent parts of a browser operation. `seamless_open_webpage_in_progress` covers identifying, activating, and preparing the destination tab. `seamless_fs_in_progress` covers moving the browser output into fullscreen. A fullscreen operation can publish both actions concurrently, and each action ends independently when its own ready point is reached.

Opening a Google Slides presentation in fullscreen:

```json
{
   "action": "seamless_open_webpage_in_progress", 
   "data": {
      "seamless_open_webpage_in_progress": true,
      "command_type": "open_url",
      "is_fullscreen": true,
      "from_tab_id": "37CD32E964DEC7F5B3B8C16B75C86AC9",
      "from_url": "https://www.presentationtools.com/",
      "to_tab_id": "782C53FB4C4FBC9CD58D7EC89B914F90",
      "to_url": "https://docs.google.com/presentation/d/example/present",
      "is_google_slides": true
   }
}
```

The associated fullscreen preparation:

```json
{
   "action": "seamless_fs_in_progress", 
   "data": {
      "seamless_fs_in_progress": true,
      "command_type": "open_url",
      "is_fullscreen": true,
      "from_tab_id": "37CD32E964DEC7F5B3B8C16B75C86AC9",
      "from_url": "https://www.presentationtools.com/",
      "to_tab_id": "782C53FB4C4FBC9CD58D7EC89B914F90",
      "to_url": "https://docs.google.com/presentation/d/example/present",
      "is_google_slides": true
   }
}
```

- `seamless_open_webpage_in_progress`: Required Boolean on the action of the same name. `true` means the destination tab is being prepared; `false` means that preparation interval has ended.
- `seamless_fs_in_progress`: Required Boolean on the action of the same name. `true` means browser fullscreen output is being prepared; `false` means that preparation interval has ended.
- `command_type`: Present on operation feedback. One of `open_url`, `switch_tab`, or `current_fullscreen`. `current_fullscreen` is used when fullscreen is requested for the current tab, including the local or network `Navigation_CurrentFS` action.
- `is_fullscreen`: Present on operation feedback. The effective Boolean fullscreen intent. This distinguishes operations such as opening or switching a tab with and without fullscreen output.
- `from_tab_id` and `from_url`: Optional cached identity and full URL of the tab APS is switching away from.
- `to_tab_id` and `to_url`: Optional identity and full URL of the destination tab. APS may initially know only the requested URL or tab ID and publish an updated active frame when Chrome confirms more exact target information.
- `is_google_slides`: Optional Boolean derived from `to_url`. It is present when the destination URL is known.

Network browser commands and equivalent actions from the local APS user interface use the same contract. Google Slides preparation appears only through these webpage actions and does not also emit `presentation_preparing`.

Active updates and the final false edge retain all available operation context. Clients must replace their stored context with every frame. A false edge reports only that the corresponding preparation interval ended; it is not a structured success result. After advertising the capability, APS includes both actions in the initial connection snapshot. An ordinary idle snapshot contains only the action's required Boolean set to `false`.

---

#### active application
The current focused application.
```json
{
   "action": "active_application", 
   "data": {
      "application": "PDF"
   }
}
```
- `application` can be `PowerPoint`, `Keynote`, `PDF`, `Webpage`, or `null`. Availability depends on the operating system and installed presentation applications.

---

#### Settings

Available in APS 4.3 and later on macOS and Windows. This feedback exposes the current Settings-tab choices, selectable displays, and detected presentation applications. A safe subset can be changed with `SetSetting` and `ToggleSetting`.

APS sends this feedback:

- as part of the full state snapshot when a client connects;
- in response to the parameterless `GetSettings` command;
- after a setting changes locally in APS;
- after every recognized remote settings write attempt, including failures and no-op writes; and
- when effective display or startup state changes, or the installed-app list is refreshed.

```json
{
  "action": "settings",
  "data": {
    "main_presenter_screen": {
      "selection": "automatic",
      "configured_display_id": null,
      "effective_display_id": 1,
      "effective_display_name": "Built-in Retina Display",
      "available_displays": [
        {
          "display_id": 1,
          "display_name": "Built-in Retina Display"
        },
        {
          "display_id": 8,
          "display_name": "Stage Display"
        }
      ]
    },
    "presentation_file_handling": "automatic",
    "seamless_switching": false,
    "run_at_system_startup": {
      "enabled": false,
      "status": "disabled"
    },
    "toggle_images_on_off_with_one_button": true,
    "powerpoint_hide_presenter": false,
    "google_slides_use_presenter_view": true,
    "pdf_controlled_program": "skim",
    "automatically_check_for_updates": true,
    "installed_presentation_apps": [
      "powerpoint",
      "keynote",
      "adobe_acrobat",
      "skim"
    ]
  }
}
```

The example above is a macOS snapshot. Windows omits `powerpoint_hide_presenter`, includes the Windows-only Boolean fields `hold_at_end_of_presentation` and `link_presentations`, and reports its Windows-specific PDF applications as described below.

- `main_presenter_screen.selection`: `automatic` or `specific`.
- `configured_display_id`: The configured platform display ID for a specific selection, or `null` for automatic selection. On Windows this is the 1-based display position shown by APS.
- `effective_display_id` and `effective_display_name`: The display APS currently resolves for presentation output. Both can be `null` if no display is available. Display IDs describe the current computer and should not be treated as portable identifiers.
- `available_displays`: The currently selectable displays in APS order. Each entry contains `display_id` and `display_name`. Use one of these IDs when selecting a specific presenter screen.
- `presentation_file_handling`: `automatic` or `controlled`.
- `seamless_switching`: Whether Seamless Switching is enabled.
- `hold_at_end_of_presentation`: Whether APS prevents a presentation from exiting when advancing past its last slide. This field is Windows-only and is omitted on macOS.
- `link_presentations`: Whether advancing past the end of one presentation opens the next presentation, and moving backward from the beginning opens the previous presentation. This field is Windows-only and is omitted on macOS.
- `run_at_system_startup.enabled`: `true` only when the platform startup entry is enabled.
- `run_at_system_startup.status`: `enabled`, `disabled`, or `unavailable`. macOS can also report `requires_approval`.
- `toggle_images_on_off_with_one_button`: Whether the same still-image action toggles an image on and off.
- `powerpoint_hide_presenter`: Whether APS suppresses PowerPoint Presenter View. This field is macOS-only and is omitted on Windows.
- `google_slides_use_presenter_view`: Whether APS uses Google Slides Presenter View.
- `pdf_controlled_program`: `skim`, `adobe_acrobat`, or `none` on macOS; `adobe_acrobat`, `adobe_reader`, `okular`, `speedf`, or `none` on Windows.
- `automatically_check_for_updates`: Whether APS automatically checks for updates.
- `installed_presentation_apps`: A fixed-order list filtered to detected apps. Possible values are `powerpoint`, `keynote`, `adobe_acrobat`, and `skim` on macOS; and `powerpoint`, `adobe_acrobat`, `adobe_reader`, `okular`, and `speedf` on Windows.

The following settings are writable. Setting names and enum values are case-sensitive, and APS does not coerce strings or numbers into Booleans.

| Setting | `SetSetting` value | `ToggleSetting` | Validation |
| --- | --- | --- | --- |
| `main_presenter_screen` | Object; see below | No | A specific display ID must appear in `available_displays`. |
| `presentation_file_handling` | `automatic` or `controlled` | No | — |
| `seamless_switching` | Boolean | Yes | On macOS, enabling without Screen Recording permission succeeds with a warning and does not open local permission UI. |
| `hold_at_end_of_presentation` | Boolean | Yes | Windows only. |
| `link_presentations` | Boolean | Yes | Windows only. |
| `toggle_images_on_off_with_one_button` | Boolean | Yes | — |
| `powerpoint_hide_presenter` | Boolean | Yes | macOS only. PowerPoint must appear in `installed_presentation_apps`. |
| `google_slides_use_presenter_view` | Boolean | Yes | — |
| `pdf_controlled_program` | macOS: `skim` or `adobe_acrobat`; Windows: `adobe_acrobat`, `adobe_reader`, `okular`, or `speedf` | No | The selected app must be installed. `none` is feedback-only. |
| `automatically_check_for_updates` | Boolean | Yes | — |

Select the presenter screen automatically:

```json
{
  "command": "SetSetting",
  "parameters": {
    "setting": "main_presenter_screen",
    "value": {
      "selection": "automatic"
    }
  }
}
```

Select a specific currently available display:

```json
{
  "command": "SetSetting",
  "parameters": {
    "setting": "main_presenter_screen",
    "value": {
      "selection": "specific",
      "configured_display_id": 8
    }
  }
}
```

For convenience, clients may copy the complete `main_presenter_screen` feedback object into `value`; APS ignores its read-only effective-display and available-display fields.

The following settings and fields remain read-only over LAN:

- `network_port`: This setting is not included in settings feedback. Change it locally in APS; a local port change restarts the server and disconnects clients.
- `run_at_system_startup`: Change it locally. macOS may require interactive approval.
- `installed_presentation_apps`: Detected by APS.
- `effective_display_id` and `effective_display_name`: Resolved by APS.

Attempts to write these fields return `read_only_setting`. Settings feedback may be absent on older APS versions or unsupported platforms.

---

#### Settings update result

APS sends this message first after every recognized `SetSetting` or `ToggleSetting` request. It then sends a complete [`settings`](#settings) message containing the authoritative state, regardless of whether the write succeeded, failed, or made no change.

Successful change:

```json
{
  "action": "settings_update_result",
  "data": {
    "success": true,
    "operation": "set",
    "setting": "seamless_switching",
    "changed": true,
    "warnings": [],
    "error": null
  }
}
```

An explicit value that already matches the current state is successful with `changed: false`. `ToggleSetting` uses `operation: "toggle"`.

Failure:

```json
{
  "action": "settings_update_result",
  "data": {
    "success": false,
    "operation": "set",
    "setting": "network_port",
    "changed": false,
    "warnings": [],
    "error": {
      "code": "read_only_setting",
      "message": "network_port can only be changed locally in APS."
    }
  }
}
```

- `success`: Whether APS accepted and applied the request.
- `operation`: `set` or `toggle`.
- `setting`: The requested setting name, or `null` when no valid string was supplied.
- `changed`: `true` only when the stored setting changed.
- `warnings`: Stable warning codes. On macOS, `screen_recording_permission_required` can be returned when remote control enables Seamless Switching without Screen Recording permission.
- `error`: `null` on success; otherwise an object with a stable `code` and a human-readable `message`.

Error codes:

- `invalid_request`: A required parameter is missing, has the wrong JSON type/value, includes a value with `ToggleSetting`, or tries to toggle a non-Boolean setting.
- `unknown_setting`: The setting name is not recognized.
- `read_only_setting`: The setting can only be changed locally or is derived by APS.
- `unavailable_value`: The requested display or application is not currently available, or the value is a feedback-only fallback such as PDF `none`.

---

#### APS info
Provides metadata about the running APS instance.
This feedback is sent when a client connects, and again whenever the configured computer tag or file-transfer availability changes.

Windows:

```json
{
   "action": "aps_info",
   "data": {
      "app_version": "4.5.0.1",
      "computer_tag": "FOH-PC",
      "platform": "windows",
      "file_transfer_port": 31602,
      "capabilities": ["build_feedback", "close_current_presentation", "file_transfer_relative_paths", "license_status", "locality_proof", "media_player_operation_result", "notification_feedback", "presentation_preparation_feedback", "set_presentation_folder_path", "transition_control_feedback", "webpage_preparation_feedback"],
      "hostname": "FOH-PC",
      "instanceId": "7f27dc2e-e189-4ab8-9e52-642ea12b808c",
      "localAddresses": ["10.0.0.20", "2001:db8::20"]
   }
}
```

macOS:

```json
{
   "action": "aps_info",
   "data": {
      "app_version": "4.5 (52)",
      "computer_tag": "Presentation Mac",
      "hostname": "stage-mac.local",
      "instanceId": "7f27dc2e-e189-4ab8-9e52-642ea12b808c",
      "localAddresses": ["10.0.0.20", "2001:db8::20"],
      "file_transfer_port": 31602,
      "capabilities": ["build_feedback", "close_current_presentation", "file_transfer_relative_paths", "license_status", "locality_proof", "media_player_operation_result", "presentation_preparation_feedback", "set_presentation_folder_path", "transition_control_feedback", "webpage_preparation_feedback"],
      "platform": "macos"
   }
}
```

- `app_version`: The APS application version.
- `computer_tag`: The user-defined computer tag. This maps to the `PC Tag` setting on Windows and the configured computer name on macOS.
- `instanceId`: A random identifier for this APS installation, not derived from hardware. Regenerates on reinstall, or when reset via `regenerateInstanceId`.
- `hostname`: The operating-system hostname, provided for human disambiguation alongside `computer_tag`.
- `localAddresses`: All IPv4 and IPv6 unicast addresses on every up, non-loopback interface, captured when the connection is established. APS reports the complete machine-wide set even when its control or file-transfer listener is restricted to one selected interface. This field is required for implementations that support instance identity, but it may be an empty array when no qualifying address exists or enumeration fails. Ordering is insignificant, and clients must treat an empty array as unavailable evidence rather than proof that two machines are disjoint.
- `platform`: The operating-system platform. Windows reports `windows`; macOS reports `macos`. Available in APS 4.3 and later.
- `file_transfer_port`: Optional integer TCP port of the HTTP file-receive endpoint (see [File transfer (HTTP)](#file-transfer-http)). The default is `31602` on Windows and macOS. The field is omitted when the endpoint is unavailable or unsupported; clients must feature-detect the field rather than infer support from an APS version.
- `capabilities`: Optional array of additive public-API capabilities supported by this APS build. Unknown entries must be ignored. `build_feedback` enables animation-build fields in [`files`](#files-feedback); `close_current_presentation` enables `CloseCurrentPresentation`; `file_transfer_relative_paths` enables folder-preserving uploads through the file-receive endpoint; `license_status` enables the informational licence/trial feedback below; `locality_proof` enables `GetLocalityProof` and `locality_proof`; `media_player_operation_result` enables structured Media Player selection and load result events; `notification_feedback` enables generic operator-facing [`notification`](#generic-notification-feedback) feedback; `presentation_preparation_feedback` enables authoritative [`presentation_preparing`](#presentation-preparing-feedback) state; `set_presentation_folder_path` enables `SetPresentationFolderPath`; `transition_control_feedback` enables the Still Images and Media Player transition commands and typed transition feedback fields; `webpage_preparation_feedback` enables authoritative [`seamless_open_webpage_in_progress` and `seamless_fs_in_progress`](#webpage-preparation-feedback) state. Clients must treat a missing entry as unsupported.

`close_current_presentation` is implemented on macOS and Windows with the same target selection, no-save, application-preserving, no-op, and ordinary-state-feedback contract.

`transition_control_feedback` is implemented on macOS and Windows with the same command and feedback contract.

`media_player_operation_result` is implemented on macOS and Windows with the same event contract and platform-neutral error wording.

`notification_feedback` is currently implemented on Windows with the shared generic event contract. Clients must feature-detect it rather than infer availability from the app version or platform; a missing capability means the feedback is unsupported.

`presentation_preparation_feedback` is implemented on macOS and Windows with the same feedback contract. Clients must feature-detect it rather than infer availability from the app version or platform; a missing capability means the feedback is unsupported.

`webpage_preparation_feedback` is implemented on macOS and Windows with the same feedback contract. Clients must feature-detect it rather than infer availability from the app version or platform; a missing capability means the feedback is unsupported.

The three instance-identity fields and `regenerateInstanceId` use the same contract on every platform. They are implemented by APS-Mac and APS-PC. Older builds and platforms that have not implemented the contract do not send these fields.

---

#### Licence status

When `aps_info.data.capabilities` contains `license_status`, APS sends an informational snapshot of the licence/trial mode it is currently enforcing locally. APS sends it during the initial connection snapshot and again whenever the state, deadline, transition reason, or closing flag changes.

Trial example:

```json
{
  "action": "license_status",
  "data": {
    "state": "trial",
    "trial_expires_at": "2026-08-05T15:20:00Z",
    "will_close": false
  }
}
```

Final feedback before APS closes because the trial ended:

```json
{
  "action": "license_status",
  "data": {
    "state": "trial",
    "trial_expires_at": "2026-08-05T15:20:00Z",
    "reason": "trial_expired",
    "will_close": true
  }
}
```

- `state`: `licensed`, `trial`, or `unknown`. `licensed` means APS is currently operating as licensed; it is not proof of a fresh online validation. `unknown` covers startup validation, incomplete selection, and inconsistent or unavailable local state.
- `trial_expires_at`: Present only for `trial`. It is the actual trial deadline as an RFC 3339/ISO 8601 UTC timestamp. Clients should calculate countdowns from this value rather than hardcoding a trial duration.
- `reason`: Optional transition reason. `license_invalidated` means a previously accepted licence became unusable; APS preserves its existing activation/trial flow rather than closing immediately. `trial_expired` means the local trial deadline was reached.
- `will_close`: Always present. It is `true` only in the final `trial_expired` feedback sent immediately before APS closes because the expired trial was not resolved.

This feedback is informational and uses the same best-effort LAN delivery as other APS feedback. It never contains a licence key, subscription identifier, customer information, or raw licence-server response, and clients must not use it to perform licence validation. A missing `license_status` capability means this feedback is unsupported, not that APS is unlicensed. This platform-neutral contract is implemented by APS-Mac and APS-PC.

---

### Windows service discovery (DNS-SD/mDNS)

APS for Windows advertises its running TCP API on the local network through multicast DNS. Discovery is additive and best-effort: clients must continue to support manual IP entry when multicast DNS is unavailable or filtered by the network.

- Service type: `_aps-api._tcp.local`
- Instance name: `APS-<instanceId>._aps-api._tcp.local`
- SRV target: the Windows hostname reported by the same provider as `aps_info.data.hostname`
- SRV port: the TCP API listener's actual bound port
- Interface scope: all interfaces (`InterfaceIndex = 0`)

The TXT record contains UTF-8 string values:

| Key | Value |
| --- | --- |
| `txtvers` | Discovery TXT schema version. Currently `1`. |
| `id` | Persistent APS `instanceId`. |
| `name` | User-defined Windows `PC Tag`. |
| `host` | Windows hostname. |
| `platform` | `windows`. |
| `apsver` | APS application version. |
| `port` | Actual bound TCP API port, equal to the SRV port. |
| `apiver` | Integer APS TCP API version. |

APS publishes only after the TCP listener reports that it is bound. It removes and republishes the advertisement after API-port, PC Tag, instance-identity, network-interface, and sleep/wake changes. Consumers should therefore tolerate short remove/add sequences and goodbye records during normal operation.

DNS-SD may automatically suffix an instance name to resolve a name collision. The advertised label is not stable identity. A client must key candidates by TXT `id`, connect to the resolved TCP endpoint, and verify the same identity in the `aps_info` handshake. Unknown TXT keys must be ignored. Consumers should validate `txtvers`, try all resolved addresses, and treat mismatched SRV and TXT ports as stale or invalid discovery data.

---

### File transfer (HTTP):

APS on Windows and macOS runs a small HTTP/1.1 server that lets a client push a single file to the machine over the LAN. The port is advertised in [`aps_info`](#aps-info) as `file_transfer_port`; if the field is absent the endpoint is unavailable or unsupported. Base URL: `http://<aps-host>:<file_transfer_port>`.

Received files are published into the APS incoming folder (`%LocalAppData%\APS\Incoming` on Windows; `~/Library/Application Support/Auto Presentation Switcher/Incoming` on macOS).

General rules:

- **Single active session.** At most one transfer session exists at a time. A new `POST /files` discards any previous session and deletes its part file; the old `transferId` then returns `404` on every endpoint.
- While a transfer is in progress the file is written to a hidden part file named `.{transferId}.part` in the incoming folder. A partially transferred file is never exposed under its final name; on successful completion any existing file at the final destination is deleted and the part file is renamed into place (overwrite by destination).
- Requests and responses use JSON (`application/json`), except chunk bodies which are raw `application/octet-stream`. Error responses have the shape `{"error": "<message>"}`.
- Error message text is informational and may differ by platform. Clients must use the HTTP status and documented stable codes such as `offset_mismatch`, `overflow`, `size_mismatch`, and `hash_mismatch`, not human-readable wording.
- Chunk bodies are limited to 32 MiB (33554432 bytes); a larger declared `Content-Length` is rejected with `413` before the body is read. `Transfer-Encoding: chunked` request bodies are not supported (`411`).
- On per-id routes, an unknown `transferId` returns `404` **before** any body validation.
- `transferId` must match `^[a-zA-Z0-9-]{8,64}$`.
- File-name rules: the name must be non-empty after trimming, at most 255 characters, must not be `.` or `..`, and must not contain `/`, `\`, NUL, or the substring `..`. On Windows the receiver additionally rejects names that are invalid on NTFS: the characters `< > : " | ? *`, control characters, reserved device names (`CON`, `PRN`, `AUX`, `NUL`, `COM1`–`COM9`, `LPT1`–`LPT9`), and names ending in a dot or space.
- When `file_transfer_relative_paths` is advertised, `POST /files` may include `relativePath`. It uses `/` as the wire separator on every platform, must be relative, must end with `name`, and every component must pass the same cross-platform name validation. Empty components, `\`, leading/trailing `/`, parent references, and destinations outside the APS incoming folder are rejected. APS creates missing subfolders only after the file hash has verified.
- There are no timeouts and no resume: chunks must be sent strictly sequentially. `GET /files/{transferId}` reports progress and is the primitive a future resume feature would build on.

#### POST /files

Starts a new session (discarding any previous one).

Request body:

```json
{
   "transferId": "b46f6f22-8f4c-4d9a-9d6e-2f6a5a2b9c1e",
   "name": "deck.pptx",
   "relativePath": "Sunday show/Slides/deck.pptx",
   "size": 1048576
}
```

- `size`: Total file size in bytes; a non-negative integer. Zero is allowed (create, then complete without sending chunks).
- `relativePath`: Optional folder-preserving destination beneath the APS incoming folder. Omit it for the legacy behavior of publishing directly into the incoming folder. Requires the `file_transfer_relative_paths` capability.

Responses:

- `201` `{"transferId": "<id>"}` — session created, empty part file written.
- `400` `{"error": "<message>"}` — invalid `transferId`, `name`, or `size`.

#### PUT /files/{transferId}/chunks

Appends one chunk. Headers: `Content-Type: application/octet-stream`, `x-offset: <bytes received so far>` (decimal, strictly sequential). Body: the raw chunk (max 32 MiB).

Responses:

- `200` `{"bytesReceived": <total>}` — chunk appended.
- `404` `{"error": "Unknown transfer."}`
- `400` — wrong content type or missing/invalid `x-offset` header.
- `409` `{"error": "offset_mismatch", "bytesReceived": <total>}` — `x-offset` does not equal the bytes received so far.
- `409` `{"error": "overflow", "bytesReceived": <total>}` — the chunk would exceed the declared `size`.
- `413` — chunk larger than 32 MiB.

#### GET /files/{transferId}

Reports session progress.

Responses:

- `200` `{"transferId": "<id>", "name": "<name>", "relativePath": "<optional relative path>", "size": <declared>, "bytesReceived": <total>}`
- `404` `{"error": "Unknown transfer."}`

#### POST /files/{transferId}/complete

Verifies and publishes the file. Request body: `{"sha256": "<64 lowercase hex chars>"}`.

On success the part file is verified against the declared size and the SHA-256 hash, any existing file with the same name is deleted, and the part file is renamed to the final name in the incoming folder.

Responses:

- `200` `{"verified": true, "name": "<name>", "relativePath": "<optional relative path>", "sha256": "<hash>", "path": "<absolute path>"}`
- `404` `{"error": "Unknown transfer."}`
- `400` `{"error": "A lowercase hex sha256 is required."}` — malformed hash (the session survives).
- `422` `{"verified": false, "error": "size_mismatch"}` — bytes received do not equal the declared size. The session and part file are discarded.
- `422` `{"verified": false, "error": "hash_mismatch"}` — computed hash differs. The session and part file are discarded.
- `500` `{"error": "<message>"}` — the destination file could not be replaced (for example it is open in PowerPoint). The session and part file are discarded; retry by starting a new transfer.

#### DELETE /files/{transferId}

Aborts the session and deletes its part file.

Responses:

- `204` — no body.
- `404` `{"error": "Unknown transfer."}`
